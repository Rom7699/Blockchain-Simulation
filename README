# MTAMiner

MTAMiner is a project designed to run on an Ubuntu-based Docker container. It's written in C and provides mining functionality.

## Prerequisites

- Docker
- Ubuntu 24.04 (for local development)
- C compiler (for local development)

## Project Structure

## Getting Started

### Building the Docker Image

To build the Docker image, run the following command in the project directory:

```sh
docker build -t mtaminer -f miner/Dockerfile.miner .