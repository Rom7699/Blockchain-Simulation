# MTAMiner

MTAMiner is a project designed to run on an Ubuntu-based Docker container.

## Prerequisites

- Docker

## Getting Started

### Building the Docker Image

To build the Docker image, run the following command in the project directory:

```sh
docker build -t mtaminer -f miner/Dockerfile.miner .